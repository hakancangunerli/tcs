import pygame 
from turtle import* 

distance=0
for x in range(0,100):
  distance += 0.1
  forward(distance)
  right(12)
  
  
  # change the degree by i, 100 how frequent it is.

#the student is asked to create a spiral for themselves by tweaking the rules of turtle spiral. 
